bot {
    discord {
        token = ${?DISCORD_TOKEN}
    }

    gemini {
        apiKey = ${?GEMINI_API_KEY}
        enabled = true
    }

    mcp {
        serverUrl = ${?MCP_SERVER_URL}
        serverUrl = "ws://localhost:3000"
        enabled = false
    }

    commandPrefix = "!"
}
